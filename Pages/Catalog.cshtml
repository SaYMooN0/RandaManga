@page
@model RandaManga.Pages.CatalogModel

<div class="catalogContainer">
    @foreach (RandaManga.Models.Manga item in Model.mangaList)
    {
        <a class="catalogMangaLink" href="@item.RusLink">
            <img src="@item.ImagePath" class="catalogImage">
            <p class="catalogMangaLabel">@item.Name</p>
        </a>
        
    }
</div>